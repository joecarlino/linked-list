var LinkedList = (function() {
  var head = null;

  var push = function(val) {
    var newNode = function(val)
      return (
      data: val.
      next: null
  };

  };
  return {
    push: push
  };
  
}());
